/**
 * Constantes compartidas entre el schema (Studio) y las queries (Server Components).
 * Este archivo NO debe importar de 'sanity': hacerlo arrastraría el bundle completo
 * del Studio hacia el servidor y rompe el render de las páginas públicas.
 */
export const AUTHOR_DOC_ID = 'author-gustavo';
